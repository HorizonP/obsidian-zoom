
# Future tasks
*This document lists features and tasks that can be carried out in the future to improve this plugin*

- [ ] BUG: when there's no text under a header section, zoom in does not work at all
- switch to Obsidian native cli for testing
- add icons (heading level and list/task) and minimal styles (text color, bold, italic) to breadcrumb for more visually intuitive path
- in the setting page, allow the plugin to detect what elements exist in the markdown editor viewport, list the selectors and let user to choose which one to hide as well
- when zoomed into a nested list item, the plugin should remove the shared indentation level across all items to make the top item looks like a first level item in the file. And this behavior should also persist through modifying the children of the zoomed in item.
- 
- [x] change plugin to a different ID to avoid confusion